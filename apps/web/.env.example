# Cognito/API接続設定。実際の値は初回デプロイ後に`cdk deploy`の出力（CfnOutput）から取得して
# .env（gitignore対象）へコピーする。デプロイ前は空のままで起動でき、実際にログインを試みた時点で
# Amplify側のエラーになる（想定内の挙動）。
VITE_USER_POOL_ID=
VITE_USER_POOL_CLIENT_ID=
VITE_AWS_REGION=ap-northeast-1
VITE_API_URL=
